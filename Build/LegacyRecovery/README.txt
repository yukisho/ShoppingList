Legacy Shopping List recovery
=============================

This compatibility loader is retired. It remains in this release only so an
update can replace older copies that may still be installed.

Do not enable or install the ShoppingList folder separately. The retired
manifest no longer loads or writes ShoppingList_Data. Existing
SavedVariables/ShoppingList.lua files are left untouched and may be kept as a
manual recovery source.
